import 'package:flutter/material.dart';

class HubScrn extends StatelessWidget {
  const HubScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hub"),
    );
  }
}
