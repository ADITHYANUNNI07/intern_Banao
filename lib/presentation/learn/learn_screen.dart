import 'package:flutter/material.dart';

class LearnScrn extends StatelessWidget {
  const LearnScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Learning"),
    );
  }
}
